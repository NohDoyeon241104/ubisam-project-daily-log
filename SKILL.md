---
name: project-daily-log
description: 협력업체로 투입된 프로젝트의 일일 진행 기록을 표준 양식의 마크다운으로 정리·저장하는 스킬. 사용자가 그날 작업한 내용을 자유롭게 적으면 깔끔한 체크리스트 형태의 daily log(.md)로 변환해 규칙에 맞는 경로에 저장한다. "일일 기록", "오늘 작업 정리", "프로젝트 진행 기록", "데일리 로그", "진행도 정리", "프로젝트 일지" 같은 요청이 나오면 반드시 이 스킬을 사용한다. Streamlit 프롬프트 생성기가 만들어준 프롬프트를 받았을 때도 이 스킬을 사용한다.
---

# Project Daily Log — 프로젝트 일일 기록 자산화

협력업체로 프로젝트에 투입된 인원이 매일의 진행 상황을 표준 양식으로 남기고, 그 기록이 쌓여 대시보드·엑셀로 재활용되게 하는 스킬이다.

역할 분담은 명확하다. 구조·경로·메타데이터는 Streamlit 앱(프롬프트 생성기)이 책임지고, 작업 내용을 깔끔한 체크리스트로 정리하는 일은 이 스킬을 따르는 Claude가 한다. 최종 검토는 사람이 한다.

## 언제 실행하나

다음 중 하나에 해당하면 실행한다.
- 사용자가 그날 한 작업을 문장으로 적고 "정리해줘 / 기록해줘"라고 할 때
- Streamlit 프롬프트 생성기가 만든 프롬프트(경로·메타데이터 포함)를 붙여넣었을 때
- 업로드된 입력 파일을 기반으로 일일 로그를 만들라고 할 때

## 실행 단계 (번호 순서대로, 누락 금지)

1. `templates/daily_log_template.md` 를 반드시 읽는다. 이 템플릿이 출력의 유일한 기준이다.
2. 입력에서 메타데이터를 확보한다 — project, date, author, status. 프롬프트에 이미 있으면 그대로 쓰고, 없으면 사용자에게 묻는다. date 기본값은 오늘이다.
3. 사용자가 자유롭게 적은 작업 내용을 항목 단위로 끊어 체크리스트로 재구성한다. 한 문장에 여러 작업이 섞여 있으면 분리한다.
4. 작업 내용을 "오늘 한 일 / 이슈·블로커 / 내일 할 일" 세 섹션으로 분류한다. 분류가 모호한 항목은 임의로 넣지 말고 가장 가까운 섹션에 배치하되, 추정한 부분은 사용자에게 한 줄로 알린다.
5. 템플릿의 YAML 머리말 + 본문 구조에 정확히 맞춰 작성한다.
6. `output/[project_id]/[YYYY-MM-DD].md` 경로로 반드시 파일을 저장한다. 프롬프트에 경로가 명시돼 있으면 그 경로를 그대로 쓴다. output 폴더는 Git에 올라가지 않으므로(.gitignore) 생성물이 공유 레포를 오염시키지 않는다.
7. 저장한 경로를 사용자에게 알려준다.

## 출력 형식

`templates/daily_log_template.md` 의 구조를 그대로 따른다. 상단 YAML 머리말은 대시보드가 파싱하므로 키 이름과 형식을 절대 바꾸지 않는다.

- `project`: 폴더 ID와 동일하게 (공백은 언더스코어)
- `date`: YYYY-MM-DD
- `author`: 담당자·협업자를 쉼표로 구분
- `status`: 정상 / 지연 / 이슈 중 하나만 (그날 하루의 상태)
- `progress`: 정수만, % 기호 없이

## 금지사항

5요소 공식의 "금지사항 명시"에 해당한다. 아래는 일일 로그 품질을 위해 반드시 지킨다.
- 이모지 사용 금지
- 볼드체·마침표 남발 금지
- "~하는 것이 중요합니다", "이게 핵심입니다" 류의 군더더기 문구 금지
- 확신의 톤 금지 — 입력에 없는 사실을 추정으로 채우지 않는다. 추정이 불가피하면 그렇게 표시한다
- `status` 에 정상/지연/이슈 외의 값 금지
- `progress` 본문에 % 기호 금지 (숫자만)

## 분량

- 각 섹션의 항목은 1~2줄 불릿 위주
- 한 항목에 여러 작업을 욱여넣지 않는다 — 작업 단위로 쪼갠다
- 비고는 선택이며, 없으면 비워둔다

## 상태 값의 두 층위 (혼동 주의)

상태가 두 군데서 쓰이는데 서로 다른 층위다.
- 프로젝트 전체 상태 = 예정 / 진행중 / 완료 → `projects.json` 에서 관리, 일일 로그에는 넣지 않는다
- 그날 하루의 상태 = 정상 / 지연 / 이슈 → 일일 로그 YAML 의 `status`

일일 로그를 만들 때 `status` 는 항상 후자(정상/지연/이슈)다.

## 입력 예시 → 출력 예시

**입력 (사용자가 자유롭게 적은 것):**
> 오전에 SeqAutoRunPTOP X=600 스톨 재현 테스트함. iMOTFComplete 리셋이 안되는 것 같음. ScannerConfig 파라미터 연결 부분 내일 필드에서 확인 예정. MarkingStartX 값 매핑 검토함.

**출력 (체크리스트로 정리된 .md):** `templates/daily_log_template.md` 구조에 맞춰 "오늘 한 일"에는 X=600 스톨 재현 테스트와 MarkingStartX 매핑 검토를, "이슈·블로커"에는 iMOTFComplete 리셋 미동작 추정을, "내일 할 일"에는 ScannerConfig 파라미터 연결 필드 확인을 각각 분리해 배치한다.

## 함께 들어있는 자원

- `templates/daily_log_template.md` — 출력 양식 (반드시 먼저 읽기)
- `workflows/workflow_dailylog.md` — CLAUDE.MD 라우팅용 2층 워크플로우 파일
- `assets/CLAUDE.md` — 1층 회사 지침서 샘플 (라우팅만)
- `assets/projects.json` — 프로젝트 관리 샘플 데이터
- `assets/app.py` — Streamlit 프롬프트 생성기 + 대시보드 + 엑셀 내보내기
- `assets/requirements.txt` — Streamlit 앱 의존성
- `README.md` — 설치부터 시연까지 순서
